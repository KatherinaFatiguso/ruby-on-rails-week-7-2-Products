

<div class="product_<%= @product.name %>">
  <h1><%= @product.name %></h1>
  <h2><%= number_to_currency(@product.price) %></h2>

  <%= image_tag(@product.image, size: "300", alt: @product.name) %>

  <br>
  <%= link_to('Edit', edit_product_path) %>
  <br>
  <%= link_to('Index', products_path) %>
</div>
